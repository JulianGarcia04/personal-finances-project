{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "pnpm build && firebase emulators:start --only functions",
    "shell": "pnpm build && firebase functions:shell",
    "start": "pnpm serve",
    "deploy": "firebase deploy --only functions",
    "genkit": "npx -y genkit start --port=4001"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@genkit-ai/google-genai": "^1.36.0",
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "firebase-admin": "^12.2.0",
    "firebase-functions": "^6.0.1",
    "genkit": "^1.36.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/json-schema": "^7.0.15",
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  },
  "private": true
}
